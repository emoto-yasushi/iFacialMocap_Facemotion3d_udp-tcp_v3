# Face Motion v3 通信仕様

[English](PROTOCOL_V3.md) · [Pythonサンプルの実行方法](README_JA.md)

この文書は、iFacialMocap・iFacialMocapTr・Facemotion3dからデータを受け取るプログラムを作る人向けの仕様書です。PCや組み込み機器で動くプログラムを「**受信側**」、データを送るアプリを「**iOS側**」と呼びます。

通信は大きく2段階です。iOS側は、最初に項目名と並び順を記した**対応表（SCHEMA）**を送ります。その後は、その順番で数値を入れた**FRAME**を送ります。UDPでは、受信側が対応表の受信確認を返すと数値の送信が始まります。TCPでは、この受信確認は送りません。

| 知りたいこと | 読む場所 |
|---|---|
| 接続して受信を始める | [2. 接続手順](#connections) |
| 送るデータを組み立てる・届いたデータを読む | [3. メッセージの形式](#messages) |
| 項目が追加・変更された場合に対応する | [4. 対応表の変更](#schema-changes) |
| データが届かない場合や再接続に対応する | [5. 通信の維持と復旧](#communication) |
| 受信データの検証条件・上限を確認する | [6. データの検証](#validation) |

**対象の通信仕様：`contract_revision = 4`。** この版は既存の要件を読みやすく整理したもので、通信形式は変更していません。元仕様の日付は2026-09-18、構成・文章の更新日は2026-09-23です。

接続手順だけで実装が完了するわけではありません。メッセージの区切り、検証、再送・復旧も、以下の規則に従って実装してください。確認済みのPythonには[既知の実装差](#known-issues)がありますが、その動作を別の仕様として認めるものではありません。

<a id="overview"></a>

## 1. 受信できるデータと用語

v3で受信するのは、BlendShapeと、頭・左右の目の数値です。リアルタイムのトラッキングと、録画をリアルタイム再生する際の送信を対象にしています。各FRAMEには、0の値も含めて全項目を入れます。前のフレームとの差分ではありません。

対応表は変更できます。同じ対応表を使う間は数値の順序が固定されますが、項目名や個数を永久に52個へ固定する方式ではありません。同時に通信する受信側は1つで、別の通信相手が使用中の転送を横取りしてはいけません。

JSONを使うのは対応表だけです。FRAMEにはバイナリの数値を入れ、項目名・JSON・CSV・Base64・圧縮・文字列の区切りは使いません。アプリ独自の処理として、各FRAMEへのACKや古いFRAMEの再送は行いません。ただし、TCP自体の確認応答・順序制御・再送は通常どおり行われます。

録画の一括転送、FBX、音声ファイル、ボディデータ、各CGソフト向けの連携処理は既存の経路を維持します。v3への対応でBluetooth機能を追加・削除・置き換えません。iOSの既存処理を維持するための注意は[付録A](#ios-notes)にまとめています。

<a id="supported-apps"></a>

### 1.1 対応アプリ

| アプリ | 最低バージョン |
|---|---|
| iFacialMocap | 1.5.3 |
| iFacialMocapTr | 1.2.6 |
| Facemotion3d | 1.4.6 |

上記以降のバージョンが対象です。それより前のバージョンには対応していません。Facemotion3dでは**Otherライセンス**が必要です。iFacialMocapTrの接続設定の既定値は、iFacialMocapと同じです。

HELLOとSCHEMAの開始情報に入れる`contract_revision`は、どちらも**4**です。それ以外の値は拒否し、別のrevisionへ自動で切り替えません。アプリのバージョン番号と`contract_revision`は別のものです。

<a id="terms"></a>

### 1.2 用語と番号の違い

1つの**メッセージ**は、共通ヘッダーと、その後ろの**本文**（payload）で構成します。**セッション**は、開始が受け付けられてからの1つの通信を指します。セッションの途中で項目が変われば、同じセッション内で対応表を更新します。

| フィールド | 何を表すか |
|---|---|
| `message_type` | HELLO・SCHEMA・FRAMEなど、メッセージの種類です。通信手順の番号ではありません。 |
| `session_id` | どの通信のデータかを識別する値です。iOS側が生成します。ただし、HELLOと未成立HELLOを拒否するERRORのヘッダーには、代わりに受信側のnonceを入れます。 |
| `client_nonce` | 返ってきた開始応答が自分のHELLOに対応するかを確認する値です。受信側が生成します。手動開始では、SCHEMAの開始情報に0を入れます。 |
| `schema_id` | 同じセッション内の、どの対応表かを識別する番号です。対応表を変更すると増やします。 |
| `sequence` | FRAMEではフレーム番号、PING/PONGでは制御の番号です。メッセージ別の規則は3.2節にあります。 |
| `schema_version` | 対応表JSONの形式の版です。この仕様では**1**です。 |
| `contract_revision` | この通信仕様を識別する値です。この仕様では**4**です。 |

「送信元」は、通信相手のIPアドレスとポートを指します。TCPでは、それに加えて接続そのものを区別します。後から同じIP・ポートで接続されても、新しい接続として扱います。

<a id="connections"></a>

## 2. 接続手順

受信側からiPhone／iPadのIPを指定する場合は、通常開始を使います。iOSアプリで受信側のIPを入力する場合は、手動開始を使います。どちらも同じメッセージ形式を使い、開始する方向が異なります。

独自実装ではなく、まずサンプルを動かす場合は、[README_JA.md](README_JA.md)のコマンドを使ってください。

<a id="ports"></a>

### 2.1 標準ポート

| アプリ | iOS側のUDP待受 | 受信側のUDP待受 | iOS側の直接TCP待受 | 受信側の手動TCP待受 |
|---|---:|---:|---:|---:|
| iFacialMocap / iFacialMocapTr | 49983 | 49983 | 49984 | 49986 |
| Facemotion3d | 49993 | 49983 | 49994 | 49986 |

通常TCPでは、受信側から接続した**1本の接続で、両方向の通信を行います**。その接続元ポートは、OSの自動割り当てで構いません。受信側の**49986番の待受も並行して開いておきます**が、これは後からの手動接続を受けるためです。通常の返信用に2本目の接続を張るわけではありません。

ユーザーが別のポートを選んだ場合は、両側の明示設定を合わせてください。保存済みの設定は上書きしません。必要なローカルポートが使用中なら、競合をエラーとして表示します。黙って別ポートへ変えたり、使用中の別プロセスを終了したりしてはいけません。

受信側とiOS側は通常別の端末なので、UDPに同じ番号を使って問題ありません。1台のPCで模擬送信と受信を動かす場合だけ、模擬送信側のポートを明示的に変えて、待受の競合を避けます。実行例はREADMEにあります。

<a id="udp-start"></a>

### 2.2 受信側からUDPで開始する

受信側は、自分の受信ポートでUDPソケットを開き、iOS側のUDPポートへHELLOを送ります。その後の送受信にも、同じ受信側ソケットを使います。

```text
受信側                                        iOS側
   |---- HELLO（種類1）------------------------>|
   |<--- SCHEMA（種類3）------------------------|
   |     対応表の全体を検証・保存する           |
   |---- SCHEMA_ACK（種類10）------------------>|
   |<--- FRAME（種類4）-------------------------|
   |<--- FRAME（種類4）-------------------------|
```

iOS側は、**HELLOを受け取ったソケットから**、実際の送信元IP・ポートへ返信します。SCHEMAには、開始の返答に必要な情報と、項目の対応表が一緒に入っています。

受信側は、対応表のすべての断片が届き、内容を検証し、読み取り位置を保存した後でSCHEMA_ACKを返します。iOS側は、正しいACKを受け取るまでFRAMEを送りません。対応表を途中で変更した場合も同じです。

図中の数字は**メッセージの種類番号**です。UDPでは1 → 3 → 10 → 4の順になり、その後のFRAMEも種類4のままです。SCHEMAやACKが届かない場合は、[5.3節](#schema-retries)の再送を行います。

<a id="tcp-start"></a>

### 2.3 受信側からTCPで開始する

受信側からiOS側の直接TCPポートへ接続し、その接続でHELLOを送ります。UDPの開始要求は使いません。

```text
受信側                                        iOS側
   |---- TCP接続 ----------------------------->|
   |---- HELLO（種類1）------------------------>|
   |<--- SCHEMA（種類3）------------------------|
   |<--- FRAME（種類4）-------------------------|
   |<--- FRAME（種類4）-------------------------|
```

iOS側はSCHEMAを先に送り、その後にFRAMEを送ります。受信側は、SCHEMAの検証・保存を終えてから、後続のFRAMEを読み取ります。**TCPではアプリ層のSCHEMA_ACKを送りません。** 通常開始でGET_SCHEMAを追加送信したり、対応表を定期的に再送したりする必要もありません。

TCPの1回の受信では、メッセージの一部だけが届くことも、複数がまとめて届くこともあります。受信回数ではなく、[3.9節](#tcp-framing)の方法でメッセージを区切ってください。対応表を保存できない場合はFRAMEを読み取らず、エラーとしてその接続を閉じます。

<a id="manual-start"></a>

### 2.4 iOS側のIP手入力モードから開始する

まず受信側を待受状態で起動します。次に、iOSアプリで受信側のIPアドレスと受信ポートを入力し、送信を開始します。この開始方法では、受信側からHELLOを送りません。

| 通信方式 | 手順 |
|---|---|
| UDP | iOS側が指定ポートへSCHEMAを送信 → 受信側が検証・保存してSCHEMA_ACKを返信 → iOS側がFRAMEを送信。 |
| TCP | iOS側が受信側のTCP待受へ接続 → その接続でSCHEMA、続いてFRAMEを送信。SCHEMA_ACKは不要です。 |

UDPでは、iOS側はSCHEMAを送ったソケットでACKを待ちます。TCPでは、接続後の制御メッセージにも同じ接続を使います。

手動開始のSCHEMAは、**開始情報の`client_nonce`が0**です。ヘッダーには新しく生成した0以外の`session_id`を入れ、最初の`schema_id`は1にします。手動開始ボタンを押すたびに新しいセッションを作ります。別の受信側が使用中の通信や既存の転送を横取りしてはいけません。

iOS側は、`actual_fps`を1〜60、`max_udp_size`を576〜1200の範囲で指定します。`lease_ms`の既定値は10000、`contract_revision`は4です。受信側は、自分が許容するFPS・UDPサイズの上限を超える値を拒否します。受信側の既定の上限は60fps・1200バイトです。対応表を更新しても、そのセッションのnonceは0のままです。この値を受け付けるのは、手動開始を許可した受信側だけです。

手動開始でも、購入状態・利用時間・アプリが前面にあることなどの条件を確認します。UDPでACKを待つ期限は、通常開始と同じ10秒です。期限に達したら、iOS側はその試行を終了し、受信確認が取れなかったことを画面に表示します。受信側は待受を続けます。再送・ACKの再返信・GET_SCHEMAを理由に、課金・試用の開始時刻やフレーム番号をリセットしません。

<a id="messages"></a>

## 3. メッセージの形式

すべてのメッセージは、同じ**40バイトのヘッダー**から始まります。その後ろが本文です。以下のサイズはバイト数で、offsetは先頭を0とした位置です。送るのは指定されたバイナリの値であり、その値を説明する文字列ではありません。

3.2〜3.7節は、分割されていないメッセージ全体の構造を説明します。UDPでの分割は3.8節、TCPでのメッセージの区切りは3.9節に従ってください。

<a id="message-type-ids"></a>

### 3.1 メッセージの種類番号

`message_type`には、次の番号を入れます。番号はメッセージの種類を区別するためのもので、何番目に送るかを表すものではありません。実装では番号を明示し、一覧の位置から自動採番しないでください。

| 10進数 | 16進数 | メッセージ | 方向 | 本文 |
|---:|---|---|---|---|
| 1 | `0x01` | HELLO | 受信側 → iOS側 | 開始要求、8バイト |
| 2 | `0x02` | **予約値：未使用・使用禁止** | — | 有効なメッセージではありません |
| 3 | `0x03` | SCHEMA | iOS側 → 受信側 | 開始情報20バイト＋対応表JSON |
| 4 | `0x04` | FRAME | iOS側 → 受信側 | BlendShapeの整数配列＋姿勢のFloat32値12個 |
| 5 | `0x05` | GET_SCHEMA | 受信側 → iOS側 | なし |
| 6 | `0x06` | PING | 受信側 → iOS側 | なし |
| 7 | `0x07` | PONG | iOS側 → 受信側 | なし |
| 8 | `0x08` | STOP | 受信側 → iOS側 | なし |
| 9 | `0x09` | ERROR | iOS側 → 受信側 | UTF-8の理由、1〜512バイト |
| 10 | `0x0A` | **SCHEMA_ACK** | 受信側 → iOS側 | なし。UDPでは必須です |

**SCHEMA_ACKは10（`0x0A`）であり、2（`0x02`）ではありません。** 10進数の10と16進数の`0x0A`は同じ数です。`0x10`は10進数の16なので、この仕様で使える種類番号ではありません。

「予約値」は、この仕様では2を使ってはいけないという意味です。種類2を送る、ACKとして受け入れる、独自の意味を割り当てる、といった扱いはしません。この一覧にない番号も無効です。[不正データの規則](#invalid-data)に従って処理してください。

使用禁止なのは`message_type=2`だけです。`schema_id`や数値データなど、その欄の規則で許されている2は使えます。欠番のための埋め草や追加手順も不要です。2を使わない経緯は[付録D.1](#history)にあります。

<a id="common-header"></a>

### 3.2 共通ヘッダー：40バイト

複数バイトの整数とFloat32は、すべて**リトルエンディアン・パディングなし**で書きます。Pythonのヘッダー定義は`struct.Struct("<4sBBHQIIIIHHI")`です。各フィールドをこの順に書き込み、Swift構造体のメモリをそのまま送らないでください。

| Offset | サイズ | 型 | フィールド | 意味 |
|---:|---:|---|---|---|
| 0 | 4 | bytes | `magic` | ASCIIの`FMV3`。バイト列は`46 4d 56 33`です |
| 4 | 1 | UInt8 | `message_type` | 3.1節の種類番号 |
| 5 | 1 | UInt8 | `flags` | FRAMEの状態。他のメッセージでは0 |
| 6 | 2 | UInt16 | `header_size` | 常に40 |
| 8 | 8 | UInt64 | `session_id` | iOS側が生成した0以外のセッションID。ただしHELLO/ERRORには下記の例外があります |
| 16 | 4 | UInt32 | `schema_id` | 対応表番号。メッセージ別の規則は下記の表を参照 |
| 20 | 4 | UInt32 | `sequence` | フレームまたは制御の番号。メッセージ別の規則は下記の表を参照 |
| 24 | 4 | UInt32 | `source_token` | FRAMEだけが使います。他は0 |
| 28 | 4 | UInt32 | `total_payload_length` | 分割前の本文全体の長さ。ヘッダーは含みません |
| 32 | 2 | UInt16 | `part_index` | 断片の番号。0から始まります |
| 34 | 2 | UInt16 | `part_count` | 断片の数。分割しない場合は1 |
| 36 | 4 | UInt32 | `chunk_length` | このパケットに入っている本文の長さ |
| 40 | 可変 | bytes | 本文 | 本文全体、またはその断片 |

**`session_id`欄に入れる値の例外：** HELLOと、まだ成立していないHELLOを拒否するERRORには、そのHELLOの`client_nonce`を入れます。それ以外は、iOS側が生成したセッションIDです。ヘッダーのこの欄は常に0以外です。手動開始を示すnonceの0は、ここではなくSCHEMAの本文に入れます。

| フィールド | メッセージ別の規則 |
|---|---|
| `flags` | FRAMEではbit 0がトラッキング中、bit 1が録画の再生中を表します。それ以外のビットは0です。FRAME以外は欄全体を0にします。 |
| `schema_id` | SCHEMA・FRAME・SCHEMA_ACKは0以外です。GET_SCHEMAは要求する表番号、または最新を求める0です。他の種類は0です。 |
| `sequence` | FRAMEはフレーム番号、PING/PONGは対応する制御番号を使います。ERRORでは番号を入れられますが、0を推奨します。それ以外は0です。再送SCHEMAも0です。 |
| `source_token` | FRAMEだけが使います。SCHEMAや制御メッセージでは0です。6.5節に詳細があります。 |

分割しない場合は、`part_index=0`、`part_count=1`、`chunk_length=total_payload_length`にします。本文が空でも、40バイトのヘッダー全体が必要です。

<a id="hello"></a>

### 3.3 HELLO：通信開始を要求する

**方向：受信側 → iOS側。種類：1（`0x01`）。サイズ：ヘッダー40＋本文8＝48バイトです。**

| 本文Offset | サイズ／型 | フィールド | 設定できる値 |
|---:|---|---|---|
| 0 | 2／UInt16 | `requested_fps` | 1〜60 |
| 2 | 2／UInt16 | `max_udp_size` | 576〜1200。FRAMEのUDPデータグラムに付く40バイトのFMV3ヘッダーを含む上限です |
| 4 | 4／UInt32 | `contract_revision` | 4 |

本文の形式は`<HHI`です。60fps・1200バイト上限の場合、本文は次のバイト列になります。

```text
3c 00 | b0 04 | 04 00 00 00
```

区切りの`|`は説明用で、送信しません。`golden_vectors.json`の`hello_hex`に、48バイト全体の例があります。

受信側は、この開始試行に使うランダムな0以外のUInt64値`client_nonce`を生成し、ヘッダーの`session_id`欄へ入れます。同じHELLOを再送するときは、同じnonceを使います。

```text
magic = FMV3              message_type = 1        header_size = 40
session_id = client_nonce
schema_id = 0            sequence = 0            source_token = 0
flags = 0                part_index = 0          part_count = 1
total_payload_length = 8                         chunk_length = 8
```

iOS側は、購入状態・利用条件・他の転送との競合などを確認します。受け付けた場合はSCHEMA、拒否した場合はERRORだけを返します。自分の開始要求に対応した拒否を受け取った受信側は、理由を表示し、待受を閉じずにWAITINGへ移ります。

対応表の作成に最初のAR更新が必要な場合、iOS側はその更新が得られた時点でSCHEMAを送ります。受信側からの追加要求は待ちません。5秒以内に準備できなければ、短いERRORを返して、保留していた開始処理を解除します。

同じ送信元／TCP接続から、同じnonce・同じ内容のHELLOが再送された場合は、同じセッションIDと現在の対応表を返します。そのために表番号・フレーム番号・利用時間制限・ACK待ち期限・確認済み状態をリセットしません。同じnonceで内容が変わった要求は拒否します。別の送信元やnonceで、使用中のセッションを置き換えてはいけません。HELLOへの返信も、5.3節のUDP対応表送信の頻度制限に従います。

<a id="schema"></a>

### 3.4 SCHEMA：数値の読み方を伝える

**方向：iOS側 → 受信側。種類：3（`0x03`）です。**

```text
[共通ヘッダー：40バイト] [開始情報：20バイト] [対応表JSON：Jバイト]
分割しない場合の合計：60 + Jバイト
```

iOS側は、新しい通信ごとにランダムな0以外のUInt64セッションIDを生成します。SCHEMAのヘッダーには、その`session_id`と現在の`schema_id`（1以上）を入れます。`flags`・`sequence`・`source_token`は0です。初回、更新、再送のどれも同じ構造を使います。

#### 開始情報

本文の先頭20バイトは`<QHHII`です。JSONは本文のoffset 20から始まります。分割していない場合は、メッセージ全体のoffset 60です。

| 本文Offset | サイズ／型 | フィールド | 意味 |
|---:|---|---|---|
| 0 | 8／UInt64 | `client_nonce` | 通常開始ではHELLOの0以外のnonceをそのまま返します。手動開始では0です |
| 8 | 2／UInt16 | `actual_fps` | 送信FPSの上限。通常開始では1〜`requested_fps`です |
| 10 | 2／UInt16 | `max_udp_size` | FRAMEのUDPデータグラムの上限。576〜要求値の範囲です |
| 12 | 4／UInt32 | `lease_ms` | 受信側から有効な制御が届かないまま待つ時間。ミリ秒単位で、既定10000、許容範囲5000〜60000です |
| 16 | 4／UInt32 | `contract_revision` | 4 |

nonce・FPS上限・UDPサイズ上限・lease・revisionは、同じセッション内で変えません。実際の出力FPSは`actual_fps`より低くても構いません。上限自体を交渉し直す場合は、新しいHELLO／セッションを使います。手動開始の値は2.4節に従います。

受信側は本文全体を検証してから、セッションと対応表を同時に採用します。一部の断片や、先に届いたFRAMEだけでは採用しません。送信元・nonceの検証や、セッションを置き換えられる条件は[6.2節](#session-validation)にあります。

#### 対応表JSON

圧縮しないUTF-8 JSONを使い、**キーは次の9個だけ**にします。この例のBlendShapeは4項目ですが、必須の項目一覧を示しているわけではありません。

```json
{
  "schema_version": 1,
  "app": "Facemotion3d",
  "profile": "facemotion3d-other",
  "blend_names": ["eyeBlinkLeft", "eyeBlinkRight", "jawOpen", "myCustomSmile"],
  "blend_encoding": "i16",
  "blend_unit": "percent",
  "pose_layout": "head_rxyz_pxyz_rightEye_rxyz_leftEye_rxyz",
  "rotation_unit": "degree",
  "position_unit": "meter"
}
```

| キー | 設定する値・役割 |
|---|---|
| `schema_version` | 1。JSON形式の版です |
| `app`、`profile` | `iFacialMocap`／`ifacialmocap-stream`、または`Facemotion3d`／`facemotion3d-other`の組み合わせです |
| `blend_names` | FRAMEの整数配列と同じ順番の項目名です |
| `blend_encoding` | `i16`または`i32`。その表の全BlendShapeに同じ型を使います |
| `blend_unit` | `percent` |
| `pose_layout` | `head_rxyz_pxyz_rightEye_rxyz_leftEye_rxyz` |
| `rotation_unit` | `degree` |
| `position_unit` | `meter` |

`schema_id`はJSONではなくヘッダーに入れます。数値型のキー名は`blend_encoding`であり、`value_type`ではありません。ライセンスやiFacialMocapTr用のキーも追加しません。上記のapp/profileの組み合わせを維持してください。

受信側は、対応表を受け取ったときに、項目名と配列位置を対応付けます。その後のFRAMEでは、その位置から値を読みます。名前・JSON・サイズの検証条件は[6.1節](#limits)にあります。

<a id="schema-ack"></a>

### 3.5 SCHEMA_ACK：対応表の保存を知らせる

**方向：受信側 → iOS側。種類：10（`0x0A`）。UDPだけで使います。サイズは40バイトで、本文はありません。**

受信側は、対応表の全体を検証・保存できた後に、このメッセージを返します。`on_schema`コールバックがある場合も、正常終了してからACKを送ります。

```text
magic = FMV3              message_type = 10       header_size = 40
session_id = 採用したSCHEMAのsession_id
schema_id = 検証・保存したSCHEMAのschema_id
flags = 0                sequence = 0            source_token = 0
total_payload_length = 0
part_index = 0           part_count = 1          chunk_length = 0
```

ヘッダーのoffset 4、つまり先頭から5バイト目に`0A`を入れます。その1バイトだけではなく、ヘッダー全体を送ってください。文字列の`"0x0A"`や`"10"`ではありません。種類2へ置き換えたり、`session_id`欄へHELLOのnonceを入れたりしないでください。

iOS側は、送信元IP・ポート、セッションID、現在の対応表番号がすべて一致し、かつ実際にその対応表を送信していた場合だけACKを受け付けます。古い表や未来の表へのACKでは、FRAMEの送信を始めません。

ACKに項目名やチェックサムを再掲しません。同じセッションID・対応表番号の組み合わせでは、SCHEMA本文全体が変わらないことを前提にしています。ACKは対応表の同期確認であり、送信元の暗号認証ではありません。保存済みの対応表へのACK再返信は5.3節に従います。ACKに対する追加のACKは送りません。

<a id="frame"></a>

### 3.6 FRAME：1フレーム分の数値を読む

**方向：iOS側 → 受信側。種類：4（`0x04`）です。** 現在の対応表にある名前の順序と数値型を使って、整数配列を読み取ります。

```text
[ヘッダー：40バイト] [BlendShapeの整数値N個] [頭＋右目＋左目：48バイト]
                      i16：2 × Nバイト
                      i32：4 × Nバイト
```

本文に項目数や数値型を追加しません。それらはSCHEMAで分かります。0も含め、全BlendShapeの値を入れます。`flags`と`source_token`はヘッダーの欄であり、本文には入りません。

#### BlendShape値

i16の対応表で次の名前が定義されている場合、整数の部分はこうなります。

| 配列位置 | 名前 | 値 | リトルエンディアンのバイト列 |
|---:|---|---:|---|
| 0 | eyeBlinkLeft | 12 | `0c 00` |
| 1 | eyeBlinkRight | 8 | `08 00` |
| 2 | jawOpen | 45 | `2d 00` |
| 3 | myCustomSmile | -25 | `e7 ff` |

値は、符号付きの整数パーセントです。係数として使う場合は100で割ります。25は0.25、-25は-0.25、150は1.5です。受信した値を復元する段階で、0〜100に切り詰めてはいけません。

i16の範囲に収まらない値を送る場合、iOS側は**その値を送る前に**、i32の新しい対応表を送ります。同じセッション内では、自動でi16へ戻しません。i32にも収まらない場合はERRORにし、上限・下限へ勝手に置き換えたり、桁あふれした値を送ったりしません。送信側の計算・丸めは既存の結果を維持します。付録Aに詳細があります。

#### 頭と左右の目

整数配列の後ろには、**Float32が12個**続きます。順序は次のとおりです。`rx/ry/rz`は回転、`px/py/pz`は位置です。

| 順番 | 対象 | 値の並び | バイト数 |
|---:|---|---|---:|
| 1 | 頭 | rx, ry, rz, px, py, pz | 24 |
| 2 | 右目 | rx, ry, rz | 12 |
| 3 | 左目 | rx, ry, rz | 12 |

回転の単位は度です。位置はメートル由来ですが、アプリの倍率・軸変換・キャリブレーション・ミラー設定を適用した後の値です。絶対的な実空間座標ではありません。profileの意味を維持し、別の共通座標系へ黙って変換しないでください。12個すべてを有限値とし、NaN・無限大・本文長の不一致は拒否します。内部の`head[6...8]`などは送信しません。

顔を見失った場合は、flagsのbit 0を0にします。最後の値を保持しても構いませんが、追跡中とは表示しません。録画のリアルタイム再生中はbit 1を1にします。録画にtracking情報がない場合、bit 0は現在のカメラの追跡状態を表し、再生データが有効かどうかとは区別します。

FRAMEの`sequence`は、セッションごとのUInt32フレーム番号です。[6.3節](#sequence)に従って、重複や古いフレームを除外します。対応表を変更しても、この番号はリセットしません。

52項目をi16で送る場合、分割されていないFRAMEは**40＋104＋48＝192バイト**です。60fpsでは、**FRAMEだけで11520バイト／秒**になります。ネットワークヘッダー、対応表、制御メッセージは別途必要です。

<a id="controls"></a>

### 3.7 その他の制御メッセージ

GET_SCHEMA・PING・PONG・STOPは、共通ヘッダー40バイトだけを送ります。本文長は0、`part_index=0`、`part_count=1`です。それ以外の欄は3.2節の規則に従います。受信側から送る制御メッセージは、HELLOを除いて採用済みのiOSセッションIDを使います。iOS側は送信元IP・ポート、またはTCP接続も照合します。

| メッセージ | 送る内容と、その後の動作 |
|---|---|
| GET_SCHEMA（5） | `schema_id`に要求する表番号を入れます。0なら最新を要求します。iOS側はその表を返し、もう保持していなければ最新表を返します。SCHEMA_ACKの代わりにはなりません。 |
| PING（6） | 受信側が制御番号を付けて送り、iOS側は同じ番号のPONG（7）を返します。送信間隔と有効期間の規則は5.7節にあります。 |
| STOP（8） | 受信側が、該当するv3セッションを明示的に終了させます。無関係の転送や旧形式の転送は止めません。復旧試行を使い切っただけでは送りません。 |

まだセッションが成立していない場合は、GET_SCHEMAではなく同じHELLOを再送します。古いFRAMEの再送は要求しません。

#### ERROR（9）

ERRORは、**iOS側から受信側へ**送ります。本文は、理由を示す**1〜512バイトの正しいUTF-8**です。追加の終端文字は付けません。`schema_id`・`flags`・`source_token`は0で、`sequence`も0を推奨します。分割しないERRORでは、`part_index=0`、`part_count=1`とし、2つの本文長欄には理由のバイト長を入れます。

| ERRORを送る場面 | ヘッダーの`session_id`欄に入れる値 |
|---|---|
| まだ受け付けていないHELLOを拒否する | そのHELLOの`client_nonce` |
| 成立済みの通信でエラーを通知する | iOS側が生成した`session_id` |

HELLOへの拒否は、nonceと想定する送信元／TCP接続が一致し、その要求が**まだ未成立の場合だけ**適用します。セッションを採用した後に、以前のHELLOへの拒否が遅れて届いても、現在の通信状態を変更しません。

正しく一致するERRORを受信したら、理由を表示してWAITINGへ移ります。受信プログラム自体は終了しません。適用する前にUTF-8と送信元・セッションを検証し、不正なERRORは6.4節に従って扱います。確認済みの受信コードには、[UTF-8検証に関する実装差](#known-issues)が残っています。

<a id="udp-framing"></a>

### 3.8 UDPメッセージの分割と組み立て

SCHEMAが1つのUDPデータグラムに収まらない場合は、本文を**536バイトずつ**に分けます。各データグラムは、40バイトのFMV3ヘッダーを含めて**最大576バイト**です。受信側はまだSCHEMAの開始情報を読めないため、この分割幅は事前に固定しています。

FRAMEは、交渉済みの`max_udp_size`を使って分けます。計算は次のとおりです。

```text
SCHEMA: capacity = 576 - 40 = 536
FRAME : capacity = max_udp_size - 40
part_count = max(1, ceil(total_payload_length / capacity))
```

各データグラムは、ヘッダーと本文の断片で構成します。最後以外の断片の本文は`capacity`バイトで、最後は残りのバイトです。断片数は**最大512**です。開始用・途中用・終了用の別メッセージ種類は作りません。

同じメッセージの全断片で、`message_type`・`flags`・`session_id`・`schema_id`・`sequence`・`source_token`・`total_payload_length`・`part_count`を一致させます。`part_index`が、断片の位置を表します。その順に本文を連結し、全体がそろってから解析します。SCHEMAの開始情報20バイトは、分割前の本文の先頭に1回だけ入り、断片ごとに繰り返しません。

同じ内容の重複断片は無視します。断片の内容やヘッダー情報に矛盾があれば、その組み立て途中のメッセージを破棄します。同じ対応表・同じ本文の再送から得た断片は、組み立てに再利用できます。不完全なFRAMEを描画へ渡してはいけません。バッファと組み立て時間の上限は6.1節にあります。

送信側も、キューを無制限に増やしません。対応表の送信処理は同時に1つ、保留する再送は最大1回とし、有限のバッチで送ります。古いモーションをためず、最新を優先します。項目数が大きい場合は、UDPのFPSを下げるか、TCPの利用を検討してください。

576バイトという上限は、どの経路でも届くことや経路MTUを保証するものではありません。信頼できるLAN向けの仕様です。特殊な経路でデータグラムが届かない場合は、TCPなど適した通信方式を使います。

<a id="tcp-framing"></a>

### 3.9 TCPでメッセージを区切る

TCPはバイト列として届くため、1回の受信を1メッセージとみなしません。受信したデータをバッファに保持し、次を繰り返します。

1. 40バイト以上そろったら、共通ヘッダーを検証します。
2. 続く`chunk_length`バイトの本文がそろうまで待ちます。ヘッダーと本文を合わせたものが1メッセージです。
3. 完全なメッセージを処理し、余ったバイトは次のメッセージ用に残します。

TCPでは、アプリ独自の分割は行いません。`part_index=0`、`part_count=1`、`chunk_length=total_payload_length`です。別の4バイト長プレフィックス、旧形式の終端文字列、対応表分割用のメッセージは追加しません。

書き込みは接続ごとに直列化します。送信を始めていないモーションは最新の1件に置き換えますが、必要なSCHEMAと、その表に従うFRAMEの順番は守ります。送信を始めたメッセージは途中で切り詰めません。同時書き込み数と受信バッファに上限を設け、停止したネットワーク処理を無限に待たないようにします。

接続を閉じたら、その接続で受信途中だったメッセージを破棄します。開始時の期限と、新しい接続でSCHEMAを検証し直す規則は[5.6節](#tcp-reconnect)にあります。

<a id="schema-changes"></a>

## 4. 対応表が変更されたとき

値が変わっただけなら、対応表を変更する必要はありません。整数配列の読み方が変わる場合に、新しい対応表番号を使います。

| 変更内容 | 新しい対応表が必要か |
|---|---|
| 値が変わる、顔を見失う、音声が反応する／止まる | 不要です。FRAMEへ反映します |
| 項目の追加・削除・名前変更・並び替え | 必要です。`schema_id`を増やします |
| BlendShapeの型をi16からi32へ変更 | 必要です。`schema_id`を増やします |
| `source_token`だけが変わる | 不要です。次のFRAMEヘッダーへ反映します |

UDPでは、新しい対応表へのACKを受け取るまでFRAMEを止めます。TCPでは、新しい表を送ってから、その表に従うFRAMEを送り、SCHEMA_ACKは待ちません。同じセッションID・対応表番号の組み合わせには、常に同じSCHEMA本文全体を対応させます。表番号を使い切った場合は、番号を別の内容へ再利用せず、新しいセッションを開始します。

<a id="schema-change-sender"></a>

### 4.1 iOS側の処理

UDPでは、**FRAMEを止める → 新しい表を送る → その表のACKを受ける → 最新の数値から再開する**、という順に処理します。

```text
iOS側 → 受信側    表7のFRAME
                  iOS側が設定変更を確定し、FRAMEを一時停止
iOS側 → 受信側    SCHEMA 8
受信側 → iOS側    検証・保存後にSCHEMA_ACK 8
iOS側 → 受信側    表8のFRAME
```

設定変更を確定したら、新しい表・数値型・項目位置を一組の固定したデータとして作ります。まだOSへ渡していない旧表のFRAMEや断片は破棄します。送信済みのパケットは取り消せません。新しい表を保存して送信し、`WAIT_SCHEMA_ACK`へ移ります。

同じ送信元・同じセッションから、現在の表に対する正しいACKが届いた場合だけ再開します。ACK待ちでも顔の計算や画面表示は続けますが、送信用の数値は最新の1組だけを保持します。ACK後は最新値から再開し、待っていた間のFRAMEをまとめて送りません。FRAMEの番号もリセットしません。

表8のACKを待っている間に表9が必要になった場合は、保留中の表を9へ置き換え、9のACKだけを待ちます。遅れた表8へのACKは無視し、異なる表の数値配列を混ぜません。短時間に変更が続いた場合は最新の表へまとめ、表の送信上限である1バッチ／秒を守ります。その連続したACK待ちの最初から数える10秒の期限は、追加の変更・再送・HELLO・PINGでは延ばしません。

TCPでは、同じ接続に、送信済みの旧データ → 新SCHEMA → 新FRAMEの順で書き込みます。ACK待ちの状態は設けません。

<a id="schema-change-receiver"></a>

### 4.2 受信側の処理

すべての断片・開始情報・JSONを検証してから、現在の対応表を置き換えます。不正な表で、検証済みの表を上書きしてはいけません。読み取り位置と型を更新し、`on_schema`を正常に終えてから、UDPのACKを返します。

新しい表を採用したら、組み立て途中の旧表FRAMEと、キューにある旧表へのACKを破棄します。その後に古い表番号のFRAMEが届いても読み取らず、旧配列を新しい読み取り位置で解釈し直すこともしません。

新しい表をまだ組み立てている間は、遅れて届いた旧FRAMEを、まだ有効な旧表で読むことはできます。ただし、2つの対応関係を混ぜません。描画側が最後の解釈済み姿勢を表示し続けることは可能です。それは、古い数値配列を新しい表で読み直すこととは異なります。

<a id="communication"></a>

## 5. 通信の維持と復旧

通信の監視には、**iOS側が対応表へのACKを待つ処理**と、**受信側が新しいFRAMEの到着を確認する処理**があります。別々の側が、別々の時刻から監視します。iOS側の送信期限が切れても、受信プログラムまで終了するわけではありません。

以下では、開始時の再試行、対応表やACKの再送、FRAMEが止まった場合の復旧、TCPの再接続を分けて説明します。

<a id="states"></a>

### 5.1 受信側の状態

| 状態 | 意味 |
|---|---|
| `WAIT_SCHEMA` | 通常開始で、最初の対応表を待っています。 |
| `WAIT_FRAME` | 最初の対応表を保存し、最初の有効なFRAMEを待っています。 |
| `STREAMING` | 新しい有効なFRAMEを受信しています。 |
| `RECOVERING` | FRAMEが止まり、回数を決めて復旧を試みています。 |
| `WAITING` | 自動試行は止めていますが、届いたデータや手動開始を受け付けられます。 |

`WAITING`でも、**PCや受信プログラムは終了しません**。受信ソケットやTCPの待受は開いたままです。また、iOS側が対応表へのACKを待つ`WAIT_SCHEMA_ACK`とは、別の状態です。

<a id="startup-retries"></a>

### 5.2 接続開始がうまくいかない場合

| 受信側で起きていること | 既定の再試行 | 上限に達したら |
|---|---|---|
| UDPで最初のSCHEMA全体が届かない | HELLOを直ちに送り、その後は1秒間隔で送ります。**初回を含めて最大5回**です。nonceと本文は同じものを使います。 | ソケットを開いたまま`WAITING`へ移ります。 |
| TCPで通常の接続を確立できない | **最大5回**試します。1回の接続待ちは標準で**3秒**、試行間隔は最低**1秒**です。 | 無限に再接続せず、受信待機を維持します。 |

現在の未成立HELLOに対応する正しいERRORが届いた場合も、理由を表示して`WAITING`へ移ります。最初の対応表がまだ完成していない場合、セッションも未確定なので、GET_SCHEMAは使いません。UDPでは同じHELLOを再送します。

開始時には、これとは別に**5秒の期限**があります。以下はそれぞれ独立した監視です。

| 監視する側 | 待っているもの | いつから数え、期限に達したら何をするか |
|---|---|---|
| 通常TCPのiOS側 | 完全なHELLO | 接続を受け入れた時点から数えます。5秒でHELLOがそろわなければ、その接続を閉じます。 |
| 開始応答を準備するiOS側 | SCHEMAを作るために必要なデータ | 5秒以内に準備できなければ、短いERRORを返し、保留中の開始処理を解放します。 |
| 新しいTCP接続の受信側 | 完全な初回SCHEMA | 接続確立時、または手動接続を受け入れた時点から数えます。5秒でそろわなければ、その接続だけを閉じ、待受は残します。[5.6節](#tcp-reconnect)も参照してください。 |

SCHEMAの準備に最初のAR更新が必要な場合、iOS側はデータが利用可能になった時点で送信します。受信側からの追加要求は待ちません。これらの規則は、ネットワークが使えない状態でも接続の成功を保証するものではありません。

<a id="schema-retries"></a>

### 5.3 SCHEMAやSCHEMA_ACKが届かない場合 — UDP

iOS側は、現在の対応表に対する正しいACKを受け取るまで、FRAMEを送らず対応表を再送します。

| 届かないもの | iOS側の処理 | 受信側の処理 |
|---|---|---|
| 初回SCHEMAの全部または一部 | FRAMEを送らず、同じ表を1秒間隔で再送します。 | 不完全な表にはACKせず、5.2節に従ってHELLOを再送します。 |
| 更新後のSCHEMAの全部または一部 | FRAMEを止めたまま、現在の表を再送します。 | 表の全体を検証・保存してからACKします。古い表から新しい並びを推測しません。 |
| SCHEMA_ACK | FRAMEを送らず、対応表を1秒間隔で再送します。 | 保存済みの表の再送だと確認したら、ACKをもう一度返します。 |
| 1件のFRAME | そのFRAMEは再送しません。 | 次に完全に届いた有効なFRAMEを使います。 |

**iOS側のACK待ちは10秒です。** 初回は対応表を送信した時点、対応表の変更時はFRAMEを止めた時点から数えます。その期限までに現在の表へのACKが届かなければ、可能な場合は`ERROR: SCHEMA_ACK_TIMEOUT`を送り、そのv3セッションを終了します。受信側の待受は終了しません。

HELLO・GET_SCHEMA・タイマーをきっかけに送るSCHEMAには、**通信相手ごとに最大1バッチ／秒**という共通の上限を使います。1バッチは「1つの表を送るための全断片」であり、断片1個ではありません。0秒に初回送信できる場合、送信機会は0、1、…、9秒の最大10回で、10秒で終了します。バッチを重ねて送りません。全制御メッセージを合計1個／秒に制限する意味ではなく、HELLO・GET_SCHEMA・SCHEMA_ACK・PINGにはそれぞれの規則を適用します。

追加の対応表変更・再送・同じHELLO・PINGでは、最初から数えている10秒の期限を延ばしません。変更が続いた場合は最新の表にまとめます。ACK待ちの間もPING/PONGとSTOPを処理します。セッションのleaseと既存の利用時間制限も別に適用し、先に満了した期限で終了します。

**受信側は**、新しい表を採用した直後にACKを1回送り、その後の再ACKは最大1回／秒にします。完全な表をすでに保持していれば、再送の断片を保存済みの本文と比較し、再送全体の到着を待たずにACKを返して構いません。これは、検証済みの全体をすでに持っている場合だけの扱いです。初回の不完全な表にACKしてよいわけではありません。同じJSONを再解析する必要はなく、ACKへの追加のACKも送りません。

受信確認後、iOS側は同じ表を約10秒ごとに再送しても構いません。その再送だけで受信確認済みの状態を取り消したり、FRAMEを止めたりしません。再送や重複ACKによって、表番号・フレーム番号・状態・利用時間を初期化することもありません。

<a id="frame-recovery"></a>

### 5.4 新しいFRAMEが届かなくなった場合

次の2つの時刻を分けて保持します。

| 時刻 | 更新する条件 |
|---|---|
| `last_valid_receive` | 通信仕様上正しいSCHEMA・PONG・FRAMEを受信したときです。 |
| `last_frame_at` | 現在の表に従い、新しいFRAMEの全体を検証・復元できたときです。 |

新しいモーションデータが届いたかどうかは、後者で判断します。PONG・対応表・ACKの再送・途中の断片・不正なFRAME・重複または逆順のFRAMEでは、`last_frame_at`を更新しません。`tracking=false`でも有効なFRAMEなら更新します。顔を見失ったことと、通信が途切れたことは別です。

最初のFRAMEが届くまでは、初回の対応表を保存した時刻を**基準時刻**にします。その後は、最後の新しい有効なFRAMEを受信した時刻を使います。

| 基準時刻からの経過 | 受信側の処理 |
|---|---|
| 新しいFRAMEが3秒届かない | 復旧試行を始めます。 |
| 復旧試行中 | 1秒間隔で最大3回試します。UDPは保存済みの現在の表へのACKと、`schema_id=0`のGET_SCHEMAを送ります。TCPはGET_SCHEMAだけを送ります。 |
| 新しいFRAMEが12秒届かない | 受信口を開いたまま`WAITING`へ移ります。タイマーによるHELLO・ACK・GET_SCHEMA・PING・自動再接続を止めます。 |
| 有効な通信がないままleaseが先に切れる、または適用対象の正しいERRORが届く | 12秒より早く`WAITING`へ移って構いません。プログラムは終了しません。 |

例えば、タイマーが予定どおりに実行される場合、復旧試行は3秒・4秒・5秒の3回です。対応表を採用した直後の最初のACKは、この3回に含めません。対応表の再送に応じて返すACKも、別の受動的な返信で、再ACKの上限は1秒に1回です。同じ秒にタイマーからのACKと返信のACKが重なった場合は、1回にまとめて構いません。

12秒という既定値は、iOS側の独立した10秒のACK待ちより余裕を持たせています。対応表更新のACKを待っているだけで、すぐに通信断とみなしません。同じFRAME未受信期間内では、新しい表番号やPONGが届いても、基準時刻と復旧回数をリセットしません。表が変わった後の再ACKは、新しい現在の表だけを対象にし、古い表へのACKはキューから取り除きます。

一度`WAITING`へ移った後は、同じ表や新しい表が届いても、使い切った自動復旧を最初からやり直しません。表を検証・保存して`on_schema`を終え、UDPならACKを返します。**新しい有効なFRAMEが届いたとき**に`STREAMING`へ戻り、通常の監視と、次の中断に備えた3回の復旧枠を初期化します。現在のUDPセッション、または切断されずに続いている同じTCP接続で有効な表が必要です。新しいTCP接続は、先に[5.6節](#tcp-reconnect)の手順を満たします。

起動直後の`--listen`は、まだ復旧回数を使い切っていません。そのため、最初の手動SCHEMAを受けた後は、上記の初回FRAME監視を始めます。

<a id="waiting"></a>

### 5.5 自動試行を止めても受信待機は続ける

**UDP：** ポートを割り当て済みの同じソケットで、`recvfrom`を続けます。`WAITING`へ移るだけでソケットを閉じたり、作り直したり、自動割り当てポートへ変更したり、STOPを送ったりしません。UDPの`connect`で通信相手を1つに固定するフィルターも使わず、送信元IPとセッションをアプリ側で検証します。同じセッションから遅れて届いたFRAMEは、表とsequenceが正しければ受信再開に使えます。

**TCP：** 生きている接続では受信を続けます。切断された場合やストリームが壊れた場合は、その接続だけを閉じ、受信側のTCP待受を残します。後からiOS側が手動接続すれば、SCHEMAから開始できます。TCP接続を受け入れただけで、FRAMEの受信が始まったとはみなしません。

`WAITING`中は、タイマーから要求やPINGを送らず、自動再接続もしません。ただし、正しい対応表が届けば、必要なUDPのACKは返します。古いiOSセッションがleaseで終了しても、受信側は後からの手動開始を待ち続けます。

<a id="tcp-reconnect"></a>

### 5.6 新しいTCP接続では、対応表を確認し直す

**新しいTCP接続では、相手のIP・ポートが前と同じでも、その接続で初回SCHEMAを受信・検証し直します。** 保存してある送信元とセッションIDだけで、準備済みと判断してはいけません。

初回SCHEMAの5秒期限は、接続が確立した時点から数えます。手動接続なら、受信側が接続を受け入れた時点です。受信完了の状態は接続ごとに管理します。以前のセッション情報や途中まで届いたデータによって、期限を無効にしたり、最初から数え直したりしません。

その接続で完全な初回SCHEMAを受信・検証し、`on_schema`が完了するまでは、FRAMEの復元・転送や`STREAMING`への移行を行いません。旧`session_id`・`schema_id`が一致し、FRAMEのsequenceだけが新しくなっていても不十分です。5秒以内に完全な初回SCHEMAが届かなければ、その接続を閉じて待受へ戻ります。同時に受け入れる接続数も無制限にしません。

切断時には、受信途中のバイト列を破棄します。切断済みの接続で検証した状態を、次の接続のFRAMEを受理する根拠には使いません。最後の解釈済み姿勢や、旧セッションを拒否するための履歴は保持できますが、初回SCHEMAの検証の代わりにはなりません。手動開始は、開始操作のたびに新しいセッションIDを使います。

この規則は、一度も切断されていないTCP接続での復帰を禁止するものではなく、UDPの待機も変更しません。初回SCHEMAを検証した後も、5.4節の回数制限付きの復旧規則を適用します。

確認対象の受信コードには、この部分の不具合が残っています。[付録C.4](#known-tcp-reconnect)を参照してください。今回の文書整理だけで、コードが直るわけではありません。

<a id="keepalive-stop"></a>

### 5.7 接続の維持と明示的な終了

受信側は、セッションを採用した後、数値待ち・復旧試行中・配信中には**3秒ごと**にPINGを送ります。iOS側は同じsequenceのPONGを返します。`WAITING`へ移った後は、定期PINGを止めます。

iOS側の**lease**は、受信側から有効な制御メッセージが来ているかを確認する期限です。`lease_ms`の間、一つも届かなければ、そのセッションを失効させます。PING/PONGは、別に監視しているFRAMEの期限や、iOS側の10秒のACK待ちを延ばしません。HELLO以外の制御には、採用済みのiOS側セッションIDを使い、iOS側はそのIDと送信元IP・ポート／TCP接続を照合します。

STOPは、受信側を明示的に終了するときに使います。再試行回数を使い切っただけでは送りません。受信プログラムは、Ctrl+C、`stop_event`、明示指定した`duration`の満了、bind失敗などの致命的なローカルエラー、コールバックの失敗でも終了できます。コールバックのエラーは、元の原因が分かる形で扱います。iOS側の停止操作・ライフサイクル・権限喪失・購入や試用の制限も、従来どおり適用します。

この待機機能は、PCのスリープ、OSによるプロセス終了、ネットワーク接続口の消失、ネットワークやファイアウォールによる遮断まで、自動復旧を保証するものではありません。

<a id="validation"></a>

## 6. データの検証と上限

受信側は、メッセージの種類だけでなく、長さ・送信元・セッション・対応表・数値を検証してから、FRAMEをアプリへ渡します。以下の上限と拒否条件も、この通信仕様の一部です。デバッグ時だけの確認ではありません。

<a id="limits"></a>

### 6.1 サイズ・名前・組み立て途中のデータ

下表の本文サイズには、**共通ヘッダーの40バイトを含めません**。

| 本文 | サイズの条件 |
|---|---|
| HELLO | 8バイト固定です。 |
| SCHEMA | 開始情報20バイトを含めて、最大262144バイトです。 |
| 対応表JSON | 最大262124バイトです。 |
| FRAME | `N × 整数1個のサイズ + 48`バイトです。最大16432バイト（`4096 × 4 + 48`）です。 |
| ERROR | 有効なUTF-8で、1〜512バイトです。 |
| GET_SCHEMA・PING・PONG・STOP・SCHEMA_ACK | 0バイトです。 |

対応表のBlendShape名は**0〜4096個**です。各名前は、大小文字を区別する一意なUTF-8文字列で、長さは**1〜255バイト**です。空文字、U+0000〜U+001F、不正なUnicodeを拒否します。標準の52個の名前に固定するための制限ではありません。

JSONのキーは、3.4節で定めた9個だけを使います。重複キー、不足・未知のキー、NaNなどの非標準定数を拒否します。app/profile・単位・姿勢の並び・数値型も指定に従う必要があります。頭・目は有限値に限り、NaNや無限大を拒否します。数値の本文サイズが対応表と一致することも確認します。

| 受信側が保持する組み立て途中のデータ | 上限・破棄までの時間 |
|---|---|
| 再構成中のメッセージ | 最大8件です。 |
| 実際に保持している断片の合計 | 最大524288バイトです。 |
| 1つのメッセージの断片数 | 最大512個です。 |
| 未完成のFRAME | 250msで破棄します。 |
| 未完成のSCHEMA | 3秒で破棄します。 |
| 新セッション候補の未完成SCHEMA | 同時に1件までとし、3秒で破棄します。 |

断片番号・個数・長さ・共通メタデータの検証には、3.8節の固定した分割幅を使います。同じ内容の重複は無視できますが、矛盾する断片やメタデータがあれば、その再構成データを破棄します。

<a id="session-validation"></a>

### 6.2 対応表とセッションを採用する条件

すべての断片・開始情報・JSONを検証し、`on_schema`が正常に完了した**後で**、セッションIDと対応表を同時に採用します。部分的な対応表や先に届いたFRAMEだけで、セッションを確定しません。不正な入力で現在の表を置き換えてもいけません。

通常開始では、SCHEMAの非0の`client_nonce`が、その開始時に送ったHELLOと一致する必要があります。手動開始の0は、手動受信を許可している場合だけ受け付けます。手動開始に対応するために、通常開始のnonce検証まで取り除いてはいけません。FPS・UDPサイズが要求値や許容値の範囲内か、leaseとrevisionが正しいかも確認します。nonce・FPS上限・UDPサイズ上限・lease・revisionは、同じセッション内では変更しません。

**新しいセッション**を採用できるのは、`WAIT_SCHEMA`または`WAITING`のときだけです。`WAIT_FRAME`・`STREAMING`・`RECOVERING`で別のセッションが通信を横取りしようとした場合は拒否します。参照実装の`--host`は、指定ホストのIPからの受信だけを許可します。`--host`なしの`--listen`は、信頼できるLAN上で任意の送信元を受け付けるという明示指定です。UDPは実際の送信元ポートへ返信します。成立後は送信元とセッションを、TCPでは接続そのものも照合します。

新しいセッションを採用したら、旧対応表・断片・sequenceの状態を破棄します。古いパケットで以前の通信へ戻らないよう、**置き換えた直近32件の送信元IP・ポートとセッションの組**を覚えておきます。新セッション候補の不完全な対応表は、同時に1件、最大3秒だけ保持します。

これらの識別値、ポートの選択、手動開始のnonce=0は、送信元を認証する仕組みではありません。信頼できるLAN／VPNで使い、ポートをインターネットへ公開しないでください。

<a id="sequence"></a>

### 6.3 遅れたFRAMEと未知の対応表

FRAMEの`sequence`は、セッション内のUInt32のフレーム番号です。最後に採用した値と比較するときは、次の計算を使います。

```text
difference = (new - old) mod 2^32
newer      = 1 <= difference <= 2^31 - 1
```

重複、古い番号・逆順のFRAME、差がちょうど範囲の半分になる値は採用しません。この計算でUInt32の上限をまたぐ場合も比較できます。番号が飛んでいても、ネットワークで失われたとは限りません。送信側が送る前に、意図的にFRAMEを捨てる場合もあります。対応表の変更でフレーム番号をリセットしません。

未知の`schema_id`のFRAMEは破棄し、最大1回／秒のGET_SCHEMAで対応表を要求します。ただし、初回のセッションIDがまだ確定していなければ、代わりにHELLOを再送します。要求された古い表をiOS側が保持していなければ、最新の表を返します。古い表やACKで現在の表を巻き戻したり、古いFRAMEの再送を要求したりしません。

<a id="invalid-data"></a>

### 6.4 不正なデータの扱い

未対応の種類番号（2を含みます）、未定義のflags、40以外の`header_size`、上限を超える宣言長、定義と合わない型・本文サイズを拒否します。対応表、Unicode、有限値、セッションについても、上記の条件を確認してからデータを使います。

不正な**UDP**データは、そのデータグラムまたは該当する再構成データを破棄します。**TCP**のストリームが不正な場合は、該当接続を閉じ、5.5節に従って受信側の待受を残します。壊れたストリームの途中で`FMV3`を探し、そこから新しいメッセージが始まると推測して処理を再開してはいけません。

形式が正しくても古いメッセージは、セッション・対応表・sequenceの規則に従って扱い、現在の状態を変えません。特に、以前のHELLOへの遅れた拒否応答を、成立済みセッションのエラーとして扱いません。ERROR本文は、未成立HELLOへの応答も含めて、UTF-8として検証します。

アプリ側のコールバック失敗は、ネットワークの再試行で処理するエラーではありません。元の原因を保持して終了し、`on_schema`が失敗した場合はACKを送りません。

<a id="source-token"></a>

### 6.5 ScrapingValueとsource_token

ScrapingValueは、**FRAMEヘッダー**の`source_token`だけに入れます。BlendShapeの項目名や対応表JSONには入れません。SCHEMAを含む、それ以外のメッセージのヘッダーでは`source_token=0`です。トークンが変わった場合は、対応表を変更せず、次のFRAMEへ反映します。

送信側は、取得した文字列を次の手順で数値に変換します。

1. 両端にあるASCIIの空白・タブ・改行（SP/TAB/CR/LF）だけを取り除きます。
2. 結果が空文字、または厳密に小文字の`none`なら、トークンを0にします。
3. それ以外は、UTF-8のバイト列にFNV-1a-32を適用します。初期値は**2166136261**で、各バイトに対して`h=((h XOR byte)*16777619) mod 2^32`を計算します。
4. 最終的なハッシュ値が0になった場合だけ、そのハッシュ値を1へ置き換えます。

元の値を初期化・更新したときに計算し、結果を保存して使い回します。毎フレームのWEBアクセスやハッシュ計算は行わず、Swiftの`hashValue`も使いません。既存のHTML・取得方法・取得に成功した値のキャッシュ動作は維持します。

トークンが0でも受信できます。これは認証・暗号化・なりすまし防止ではありません。6.2節のとおり、信頼できるネットワークで利用してください。

<a id="ios-notes"></a>

## 付録A. iOS側の既存実装を変更する際の注意

この付録は、iOSアプリ側を改修する人向けの内容です。受信プログラムだけを作る開発者が、これらのiOS内部処理を変更する必要はありません。既存アプリへv3を組み込む際には、以下の互換性と計算方法を維持します。

<a id="ios-compatibility"></a>

### A.1 既存の通信経路と設定を維持する

v3専用の待受ポートを増やさず、既存アプリの受信口を使います。先頭が`FMV3`のデータをv3処理へ渡し、それ以外は従来の処理へ渡します。ただし、不正なv3データを従来の文字列コマンドとして読み直してはいけません。

| 既存の経路 | 維持する内容 |
|---|---|
| iFacialMocapの従来のTCP開始 | UDP49983へ旧開始文字列を送り、iOS側から受信側のTCP49986へ接続する方式を残します。PCから開始するv3のTCPとは別経路です。 |
| iFacialMocapの直接TCP | `startListener()`が使う既存のTCP49984を共用します。別の49985の待受と、49987の録画データ経路は変更しません。 |
| Facemotion3dのUDP | iOSの標準コマンド待受49993を維持します。受信側の49983は、Otherの既定設定と公式Python例に合わせたものです。 |
| Facemotion3dの既存の自動接続分岐 | 受信側のUDP49993へ送る既存分岐があります。その従来分岐を49983へ変更しません。v3受信側も、必要な場合は`--listen-port 49993`を明示できます。 |
| Facemotion3dの互換用待受 | iOS側の既存UDP49983は、互換用として残し、閉じたり移したりしません。 |

手動送信では、ユーザーが選んだ宛先を使います。v3セッションで一時的に使う接続情報によって、従来の既定値や、保存済みの`sendPort`・`sendProtocol`設定を上書きしません。

録画一括取得・FBX・音声ファイル転送・ボディデータ・各DCCソフト向けの連携処理は、従来の経路を残します。v3対応を理由にBluetooth機能を追加・削除・置き換えたり、別クライアントの使用中の転送を横取りしたりしません。

<a id="ios-calculations"></a>

### A.2 対応表の作り方と計算結果を維持する

設定が変わったときに、送信する項目の順番を作り、保存して使い回します。毎フレームDictionaryを列挙して、その都度の順番を使ってはいけません。標準項目・`FM_*`・設定済みの音声カスタムBlendShapeを含め、発声中に反応している項目だけの表にはしません。

反応していない独自項目も、値0で表に残します。音声によって標準項目を意図的に上書きする場合は、同じ1項目を使い、同じ名前を重複して追加しません。上書きが反応していないときは、元の値を維持します。リマップなどで意図しない重複名が生じた場合は診断し、曖昧な表を送信しません。

値の丸めは、既存の`Int(weight * 100)`・`blendShapePercent`の結果を共用し、計算順序や精度を勝手に変更しません。頭・目は既存の軸・倍率・キャリブレーション・ミラー適用後の値を送り、profileの意味を維持します。別の共通座標系へ変更したり、内部で持つ追加のhead値を送ったりしません。

<a id="ios-conditions"></a>

### A.3 既存の利用条件を適用する

通常開始・手動開始のどちらでも、iOS側は購入・ライセンス状態、利用時間、前面動作の条件、権限、ほかの転送との競合を確認します。v3を使って、これらの制限を回避できるようにしてはいけません。

対応表のACK待ちでも、顔の計算と画面更新は続け、送信する数値は最新のものだけを保持します。ACKの期限が切れたら、受信確認できなかった送信試行を終了します。手動開始の場合は、確認できなかった旨をユーザーへ表示します。後から再び手動開始するときは、新しいセッションIDを使います。

再送・再ACK・GET_SCHEMA・同じHELLOによって、購入や試用の開始時刻、フレーム番号、利用時間制限をリセットしません。ACK待ち・lease・利用時間のうち、先に期限が来た条件で停止します。iOSのライフサイクル、権限喪失、アプリの停止ボタンによる従来の停止処理も維持します。

<a id="python-notes"></a>

## 付録B. 参照コード・図との対応

`face_motion_v3.py`は受信側の参照実装です。`simulate_ios_v3.py`は、iPhoneなしで試すための合成データ送信プログラムで、送信側のACK待ち・再送は内部の`SchemaDelivery`にあります。製品のiOS実装をすべて再現するものではありません。コードを別実装の参考にする前に、[付録Cの既知の実装差](#known-issues)も確認してください。

仕様書とPythonでは、次のように名前が異なる部分があります。

| 仕様書での名前 | 参照Pythonでの名前 |
|---|---|
| `message_type` | `Packet.kind` |
| `total_payload_length` | `Packet.total_length` |
| 1パケットの`chunk_length` | そのパケットの`Packet.payload`のバイト長 |

`V3Client`は手動受信待機を許可しています。下位の`ReceiverCore`を直接使う場合は、`allow_push=True`を明示したときだけ、手動開始のnonce=0を受け付けます。

| 受信側のオプション | 意味 |
|---|---|
| `--app ifacialmocap` / `--app facemotion3d` | アプリごとの既定値を選びます。省略時はiFacialMocapで、Trも同じ既定値を使います。 |
| `--transport udp` / `--transport tcp` | 通信方式を選びます。 |
| `--port` | iOSまたは模擬送信側の接続先ポートを上書きします。 |
| `--listen-port` | 受信側のローカル待受ポートを上書きします。 |
| `--host` / `--listen` | ホストへ通常開始するか、手動開始を待つかを選びます。ホストを指定した場合は、そのIPへの受信制限も適用します。 |
| `--bind` | 自分の待受アドレスと、IPv4／IPv6のどちらか1種類を選びます。既定はIPv4、IPv6は`--bind ::`です。 |
| `--no-reconnect` | 互換性のために受け付ける指定です。有無にかかわらず、自動試行には上限を設け、終了後も受信待機を維持します。 |

コマンドとアプリへの組み込み方は[README_JA.md](README_JA.md)を参照してください。Windows・macOS・IPv6の実機検証は別途必要です。

[送信図](diagrams/FMV3_PC_to_iOS_EN.png) ／ [受信図](diagrams/FMV3_iOS_to_PC_EN.png) ／ [図の英語テキスト](diagrams/DIAGRAM_TEXT_EN.md) ／ [期待バイト列](golden_vectors.json)

図の`HELLO=1 / SCHEMA=3 / FRAME=4`は一部の種類の例で、完全な一覧でも通信順序でもありません。SCHEMA_ACKは10です。また、`HELLO only: PC client_nonce`はPC→iOSだけの説明です。双方向の共通ヘッダーとしては、未成立HELLOを拒否するERRORもnonceを使います。完全な定義は第3章を参照してください。

図や期待バイト列は参照資料であり、実行に必要なファイルではありません。小規模サンプルには、保守用の自動テスト一式は含めていません。

<a id="known-issues"></a>

## 付録C. 確認済みPythonに残る実装差

**この付録はコードの観察結果であり、別の通信規則ではありません。** 2026-09-22の確認で報告された、下記のファイルに対する内容です。その後のGitHubの版や、製品iOSでの状態を断定していません。今回の文書整理では、コード自体を変更していません。

| 確認対象ファイル | SHA-256 |
|---|---|
| `face_motion_v3.py` | `d68b145ddf7307f79146cf2d1b93f691246c215a329e89593d73c766dfd0f432` |
| `simulate_ios_v3.py` | `b4d73d14b532470820eace853c5347d059c3a6698bd3e23e40c959dff45ce9f4` |

<a id="known-error-utf8"></a>

### C.1 未成立HELLOへのERRORだけ、UTF-8の検証が緩い

`V3Client._handle()`では、未成立HELLOへのERRORに`decode('utf-8', 'replace')`を使っています。ほかの照合条件が一致すれば、不正なバイトを置換文字で表示して`WAITING`へ移ります。一方、成立後の`ReceiverCore.accept()`は不正なUTF-8を拒否します。本文を1バイトの`FF`にした試験で、この違いが再現されています。

**本来の要件：** 3.7節・6.4節に従い、どちらもUTF-8として検証します。この緩い処理を、仕様として移植しないでください。別件である「以前のHELLOへの遅延ERROR」の修正済み処理は、過去のレビューで回帰確認されています。

<a id="known-token-cache"></a>

### C.2 模擬送信側が毎フレームsource_tokenを計算する

模擬送信側のFRAMEループは、毎フレーム`source_token("hapihapi")`を呼んでいます。送信される値は正しいものの、必要なキャッシュの最適化を示すコードにはなっていません。

**本来の要件：** 6.5節どおり、製品の送信側は元の値の初期化・更新時に計算し、結果を使い回します。毎フレームのハッシュ計算を、そのまま製品へ移植しないでください。

<a id="known-hello-timeout"></a>

### C.3 模擬送信側にHELLO待ちの5秒期限がない

模擬送信側のTCP受付処理には、HELLOの5秒期限がありません。HELLOを送らず5秒以上待っても接続が開いたままで、その後に送ったHELLOを受け付けます。

**本来の要件：** 通常TCPのiOS側は、接続を受け入れてから5秒以内に完全なHELLOが来なければ、その接続を閉じます。5.2節の送信側の期限であり、次の項目で説明する受信側の初回SCHEMA期限とは別です。

<a id="known-tcp-reconnect"></a>

### C.4 新しいTCP接続で、古いセッション状態を使ってしまう

手動TCPでSCHEMAとFRAMEを受信してから切断し、同じ送信元IP・ポートで新しく接続すると、古いセッションと同じものとして扱われる場合がありました。以前の確認では、次の2つが再現されています。

- 新しい接続で初回SCHEMAを送らなくても、約**5.55秒**開いたままで、その後の表を受け付けました。5秒の期限が働いていません。
- **新しい接続で一度もSCHEMAを送っていないのに**、旧セッションID・表番号と、新しいsequenceのFRAMEが受理され、`STREAMING`へ戻りました。

ソケットとTCPの組み立て用バッファは作り直されていますが、`core.session_id`と`_peer`が残り、期限と現在のセッションの判定が、新しい接続を十分に区別していません。確認ではLinux上で送信元ポートを再利用させています。通常利用での発生頻度や、製品iOSへの影響は調べていません。

**本来の要件：** [5.6節](#tcp-reconnect)どおり、接続ごとに初回SCHEMAの検証と期限を管理します。保持済みの状態だけで、新しい未検証の接続のFRAMEを受け入れてはいけません。これは受信コードの不具合であって、SCHEMAを省略してよいという意味ではありません。一度も切断されていない同じTCP接続での復帰は可能です。

模擬通信に成功しても、製品の全要件を満たした証明にはなりません。アプリのライフサイクル、購入制限、実機のネットワークは別途確認する必要があります。

<a id="history-and-tests"></a>

## 付録D. 設計の経緯と実装時の確認項目

設計の経緯は、現在の割り当てを説明するものです。古い通信形式の使用を認めたり、別の開始方法を追加したりするものではありません。確認項目は実装者・保守者向けであり、特定の製品ですべて実施済みという報告ではありません。

<a id="history"></a>

### D.1 種類番号2が未使用になっている理由

以前のv3案では、種類2を**WELCOME**という「iOS側が開始要求を受け付けたと返すメッセージ」に使っていました。その後、この独立した返答を廃止し、開始情報をSCHEMAへまとめました。ほかの種類番号は変えず、SCHEMA_ACKは10のまま、2は別の意味に再利用せず残しています。

WELCOMEはiOS側が開始要求を受け付けたという返答で、SCHEMA_ACKは受信側が対応表を検証・保存したという返答です。役割も方向も違います。`contract_revision = 4`ではWELCOMEを実装せず、種類2を送信・受理しません。

この「予約値」は、**未使用・使用禁止**という意味です。独自メッセージへ自由に割り当てられる番号でも、将来の機能追加を約束した番号でもありません。欠番のために、追加のバイト・埋め草・空パケット・待機手順・バッファ枠を作る必要はありません。ほかのフィールドで許されている値2を禁止することもありません。これはv3設計時の経緯で、文字列方式のv1/v2やアプリの版番号とは別です。また、古い設計との互換性を追加するものでもありません。

<a id="acceptance-tests"></a>

### D.2 実装時に必要な確認

| 確認する範囲 | 確認内容 |
|---|---|
| 種類番号とバイト配置 | ACKは本文なしの40バイトで、offset 4が`0x0A`です。`0x02`・未知の種類・4以外のrevisionを拒否します。予約値用の埋め草や通信手順は追加しません。 |
| 初回UDP開始 | HELLOに対してSCHEMAを送り、現在の表への正しいACKより前にFRAMEを送らないことを確認します。部分的・不正な表にはACKしません。 |
| 表やACKの欠落 | SCHEMA全体・一部を失う場合と、最初のACKだけを失う場合を分けて試します。いずれも表の再送と受信確認で復帰する必要があります。 |
| 対応表変更 | 追加・削除・名前変更や並び替え・i16→i32・ACK待ち中の再変更・重複SCHEMA/ACK・遅延した旧FRAMEを試します。表番号・セッション・送信元が違うACKで送信を再開せず、最新値から再開します。 |
| 送信側の期限 | PING・同じHELLO・連続する設定変更で、10秒のACK期限を延ばさないことを確認します。通常TCPでHELLOが不完全な接続は、5秒で閉じます。 |
| 受信側の監視 | ACK後にFRAMEが0件、配信中の中断、表更新待ち、SCHEMAだけの継続を試します。PONGだけが35秒以上続いても、受信側は回数制限付きの復旧後にWAITINGへ移っている必要があります。PONGでFRAMEの期限を延ばしません。 |
| 待機と手動再開 | UDPポートとTCP待受を維持します。タイマーによる要求やPINGを止め、待機するだけでSTOPを送りません。正しい保存済み表や新しい手動表にACKし、有効なFRAMEで復帰します。両方式で、HELLOなしの`--listen`開始を試します。 |
| TCP開始と再接続 | 通常はSCHEMA_ACKなしでHELLO→SCHEMA→FRAMEになります。切断後、同じ送信元IP・ポートの新接続でも初回SCHEMAの5秒期限を守り、SCHEMA前の旧セッションFRAMEを転送しません。正しい新しい手動SCHEMA/session→FRAMEなら開始できることも確認します。 |
| セッションとERROR | 違うIP・nonce、旧セッション、使用中の横取りを拒否します。一致する未成立HELLOへのERRORは処理し、セッション採用後の古いHELLOへの遅延ERRORは無視します。UTF-8も検証します。 |
| アプリのコールバック | `on_schema`が失敗した場合、ACKを送らず、元のエラーを保持して終了します。 |
| 数値とアプリの挙動 | 負数・100超・Unicode・型の境界・頭や目の倍率・ミラー・再生・顔喪失・停止／再接続・購入制限・従来v1/v2の動作を確認します。 |

バイト列の比較には`golden_vectors.json`、基本的な模擬通信にはREADMEの2ターミナル手順を使います。小規模配布には、保守用の自動テスト一式は含めていません。iOSの実装完了には、Xcodeでのビルドと、両アプリ系統の実機UDP／TCP相互接続も必要です。未実施の確認を成功として扱わないでください。

<a id="references"></a>

### D.3 一般的な参考資料

以下は一般的な仕組みの参考資料です。FMV3固有のフィールド・番号・動作は、本書で定義しています。

[Python struct](https://docs.python.org/3/library/struct.html) ／ [Python Socket HOWTO](https://docs.python.org/3/howto/sockets.html) ／ [UDP Usage Guidelines — RFC 8085](https://www.rfc-editor.org/rfc/rfc8085.html) ／ [TCP — RFC 9293](https://www.rfc-editor.org/rfc/rfc9293.html)
